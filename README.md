# Cmonitor 文档

https://cmonitor-document.pages.dev/

## Cloudflare Pages

- 构建命令：`npm run build`
- 输出目录：`dist`
- 根目录：`/`
- Node.js：24（仓库已提供 `.node-version`）

使用 Pages 的 Git 仓库集成时无需填写额外的 Wrangler 部署命令。
